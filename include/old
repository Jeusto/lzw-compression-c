#include <argp.h>
#include <ctype.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include <time.h>
#include <unistd.h>

#include "../include/hashmap.h"
#include "../include/hex.h"
#include "../include/liste.h"
#include "../include/trie.h"
#include "../include/utils.h"

void initialiser_dictionnaire(void *const structure, int type);
void liberer_dictionnaire(void *const structure, int type);
void ajouter_dictionnaire(void *const structure, int type, char *mot);

/**
 * @brief Gere l'execution du programme
 *
 * @param argc : Le nombre d'arguments de la ligne de commande
 * @param argv : Les arguments
 * @return int : 0 si le programme a reussi
 */
int main(int argc, char *argv[]);