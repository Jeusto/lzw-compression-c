1. Implementer & tester liste chainee // Jodel
2. Implementer & tester trie // Arhun
3. Tester hashmap // Ensemble
4. Implementer & tester dictionnaire // Ensemble
5. Faire le makefile // Ensemble
6. Implementer & tester la compression lzw // Arhun
7. Implementer & tester la decompression lzw // Jodel
8. Faire la fonction main qui correspond au programme
9. Creer des tests pour verifier le fonctionnement
10. Evaluer les performances avec les tests
11. Ecrire le rapport
12. Rendre le code plus propre, gestion d'erreurs, preconditions etc

### Executer le code

make && bin/lzw

### Pour voir les erreurs et fuites de memoires dans un fichier

make && make valgrind

### Utilisation du programme:

- bin/lzw [options] <fichier1.???>
- par defaut :
  - chosir la meilleure structure de donnees en fonction de la taille
  - compresser <fichier1.txt> placer resultat dans <fichier1.lzw>
  - ou decompresser <fichier1.lzw> placer resultat dans <fichier1.txt>
- options :
  - l = utiliser liste chainee
  - t = utiliser trie
  - h = utiliser hashmap
